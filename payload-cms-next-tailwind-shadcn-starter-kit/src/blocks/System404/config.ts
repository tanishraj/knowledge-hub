import { createSystemPageBlock } from '../systemPageFields'

export const System404Block = createSystemPageBlock({
  singularLabel: 'System 404',
  pluralLabel: 'System 404 Blocks',
  slug: 'system404',
})
